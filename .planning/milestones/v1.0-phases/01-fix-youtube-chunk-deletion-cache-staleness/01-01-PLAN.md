---
phase: 01-fix-youtube-chunk-deletion-cache-staleness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/study/[topic]/page.tsx
autonomous: true
requirements:
  - BKFL-01
  - BKFL-02
  - BKFL-03
  - CACH-01
  - CACH-02

must_haves:
  truths:
    - "Backfill sends YouTube URLs (from Firestore source records) alongside file data to /api/sources/index"
    - "replaceIndexedChunks receives YouTube transcript chunks when backfill fires"
    - "YouTube chunk documents in Firestore survive a full page reload"
    - "sourceTruthVersion increments after handleAddSourceFromUrl successfully adds a YouTube source"
    - "Cached study content is regenerated (not served stale) after a YouTube source is added"
  artifacts:
    - path: "frontend/src/app/study/[topic]/page.tsx"
      provides: "Fixed backfill logic and cache invalidation"
      contains: "listStudySources.*youtubeUrl"
  key_links:
    - from: "backfillSources useEffect"
      to: "listStudySources"
      via: "async call before /api/sources/index fetch"
      pattern: "listStudySources.*youtube"
    - from: "handleAddSourceFromUrl success path"
      to: "setSourceTruthVersion"
      via: "increment call after sources refresh"
      pattern: "setSourceTruthVersion.*current.*\\+ 1"
---

<objective>
Fix the two root causes that break YouTube RAG retrieval: (1) backfill race condition that deletes YouTube chunks on page load, and (2) missing cache invalidation after adding a YouTube source.

Purpose: YouTube transcript chunks must survive page loads and trigger cache regeneration so RAG-generated study content includes video material.
Output: Patched `page.tsx` where backfill includes YouTube URLs from Firestore and handleAddSourceFromUrl increments sourceTruthVersion.
</objective>

<execution_context>
@/Users/niharshah/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/niharshah/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/app/study/[topic]/page.tsx
@frontend/src/lib/firestore/chunks.ts
@frontend/src/lib/firestore/sources.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backfill to include YouTube URLs from Firestore sources</name>
  <files>frontend/src/app/study/[topic]/page.tsx</files>
  <action>
In the `backfillSources` function (starts at line 833), the bug is on line 874: `youtubeUrls: []` is hardcoded empty. The fix must fetch existing YouTube source records from Firestore and pass their URLs to `/api/sources/index`.

**Steps:**

1. Inside `backfillSources()`, BEFORE the existing `toast.loading` call (line 851), add an async call to `listStudySources(user.uid, strategyId)` to fetch existing Firestore source records.

2. Extract YouTube URLs from the loaded sources:
   ```typescript
   const existingSources = await listStudySources(user.uid, strategyId);
   const existingYoutubeUrls = existingSources
     .filter((s) => s.type === "youtube" && s.youtubeUrl && s.status !== "error")
     .map((s) => s.youtubeUrl!);
   ```

3. If there ARE existing sources in Firestore (either from `existingSources.length > 0` OR the sources state hasn't loaded yet), we need to be careful. The backfill guard at line 841 checks `sources.length > 0` — but `sources` is the React state that starts as `[]` and only populates after `loadSources` finishes. The existing guard `sourcesBackfillAttemptedRef` (line 838) prevents re-runs. The real fix is: if we found existing Firestore sources that include YouTube, include those YouTube URLs in the backfill call.

4. Replace the hardcoded `youtubeUrls: []` on line 874 with `youtubeUrls: existingYoutubeUrls`.

5. `listStudySources` is already imported in this file (check imports at the top — it's used in `loadSources` on line 823). If not imported, add the import from `@/lib/firestore/sources`.

**What NOT to do:**
- Do NOT modify `replaceIndexedChunks` or `appendIndexedChunks` — those functions work correctly; the bug is in the caller.
- Do NOT change the `sources.length > 0` guard logic at line 841 — this guard correctly prevents backfill when sources are already loaded into React state.
- Do NOT add YouTube URLs from `contextFiles` — `contextFiles` only contains file references, never YouTube URLs. That's part of the bug.
- Do NOT change `websiteUrls: []` — URL sources are out of scope per requirements.

**Key insight:** The backfill fires because `sources` (React state) starts as `[]` and `loadSources` hasn't populated it yet. But YouTube sources DO exist in Firestore — they were added earlier via `handleAddSourceFromUrl`. The fix fetches them from Firestore directly within backfill, bypassing the race condition with the React state.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>
      1. Add a YouTube source to a study topic via the URL input
      2. Verify chunk count appears in source list
      3. Reload the page (full refresh, Cmd+R)
      4. Open browser DevTools → Network tab
      5. Confirm the POST to /api/sources/index includes the YouTube URL in the youtubeUrls array
      6. Check Firestore console → users/{uid}/strategies/{strategyId}/indexedChunks — YouTube chunks (section matching "Transcript N") still exist after reload
    </manual>
  </verify>
  <done>
    - backfillSources calls listStudySources to get existing YouTube URLs from Firestore before the /api/sources/index fetch
    - youtubeUrls field in the fetch body contains URLs from Firestore youtube-type source records
    - TypeScript compilation passes with no new errors
    - YouTube chunk documents survive page reload (not deleted by replaceIndexedChunks)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add cache invalidation after YouTube source addition</name>
  <files>frontend/src/app/study/[topic]/page.tsx</files>
  <action>
In `handleAddSourceFromUrl` (starts at line 1057), after a YouTube source is successfully added and sources are refreshed, `setSourceTruthVersion` is never called. This means cached study content generated without YouTube context continues to be served.

**Steps:**

1. Locate the success path in `handleAddSourceFromUrl` — after line 1226 (`setSources(mergeSessionSources(refreshed, contextFiles))`) and before line 1227 (`setSourceAddStatus("completed")`).

2. Add `setSourceTruthVersion((current) => current + 1);` — this is the exact same pattern used in `handleToggleSource` at line 990.

3. Also add the cache/state reset calls that `handleToggleSource` uses (lines 991-1002), to ensure stale content is cleared:
   ```typescript
   setSourceTruthVersion((current) => current + 1);
   setChatHistory([]);
   setQuickActions({
     difference: { loading: false, content: "" },
     example: { loading: false, content: "" },
     examQuestion: { loading: false, content: "" },
     explainSimply: { loading: false, content: "" },
   });
   setLearnedItems({});
   setMicroQuizzes({});
   setExamMode(null);
   setExpandedOriginalQuestions({});
   ```

**What NOT to do:**
- Do NOT increment sourceTruthVersion inside the error/retry paths — only on successful addition.
- Do NOT call setSourceTruthVersion BEFORE the source is saved to Firestore and chunks are appended — it must come after the data writes succeed.
- Do NOT change the existing `appendIndexedChunks` call — it correctly adds YouTube chunks without deleting existing ones.

**Why this works:** `sourceTruthVersion` is embedded into cache signatures (lines 1433, 1436, 1634-1635). When it increments, all cache keys change, forcing fresh study content generation that now includes the newly-added YouTube source's chunks in RAG retrieval.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>
      1. Navigate to a study topic that already has generated study content
      2. Add a YouTube video URL via the source input
      3. Wait for "Source added" toast
      4. Observe that study content automatically refreshes (loading state appears, new content generated)
      5. New study content should reference or incorporate YouTube video material
    </manual>
  </verify>
  <done>
    - setSourceTruthVersion((current) => current + 1) is called in handleAddSourceFromUrl after successful source addition
    - Cache/state reset (chatHistory, quickActions, learnedItems, microQuizzes, examMode) happens alongside version increment
    - TypeScript compilation passes with no new errors
    - Adding a YouTube source triggers study content regeneration (not served from stale cache)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd frontend && npx tsc --noEmit`
2. Full page reload preserves YouTube chunks in Firestore
3. Adding a YouTube source increments sourceTruthVersion and triggers content regeneration
4. Existing file-based RAG workflow continues to work (no regression)
</verification>

<success_criteria>
- YouTube chunks survive page reload (backfill includes YouTube URLs from Firestore)
- sourceTruthVersion increments after adding a YouTube source
- Study content regenerates with YouTube context after adding a YouTube source
- No TypeScript compilation errors introduced
- File upload and existing source toggle functionality unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-youtube-chunk-deletion-cache-staleness/01-01-SUMMARY.md`
</output>
